meta {
  name: Get All Variables
  type: http
  seq: 11
}

get {
  url: {{url}}/sentence/{{selectedSentence}}/variable
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
