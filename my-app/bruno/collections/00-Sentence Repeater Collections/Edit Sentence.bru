meta {
  name: Edit Sentence
  type: http
  seq: 7
}

patch {
  url: {{url}}/sentence/{{selectedSentence}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {"sentence":"ABC{}","variables":{"variableName":"Tikus"}}
}

settings {
  encodeUrl: true
  timeout: 0
}
